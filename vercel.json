{
    "version": 2,
    "builds": [
        {
            "src": "server/devServer.js",
            "use": "@now/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/devServer.js"
        }
    ]
}